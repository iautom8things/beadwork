# Decisions

Durable, cross-cutting ADRs for beadwork. One decision per file, frontmatter with
`id`, `status`, `date`, `affects` (subject ids). Record a decision here only when it
constrains more than one subject or will matter months from now; single-subject
rationale belongs in the subject spec's prose.
